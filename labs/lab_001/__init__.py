"""Lab-001: workflow router vs 3-agent hierarchy on short-horizon support queries.

A free, fully local, reproducible re-run of the Lab-001 experiment using Ollama
models. See README.md for the reproduce recipe and the honesty caveats (different
models from the original Claude run, so numbers are not comparable; local
inference has no dollar cost, so the cost axis is tokens and model calls).
"""
